[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# analyze.js가 Functions v2 형식(config.path = "/api/analyze")으로
# 자체 라우팅되므로 별도의 /api/* 리다이렉트는 필요 없다.

# SPA 라우팅: 나머지 모든 경로는 index.html로
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
